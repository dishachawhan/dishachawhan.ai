import heapq

def astar(graph, H, source, goal):
    open_list = []
    heapq.heappush(pq, (h[source], source))
    cost_so_far = {source: 0}
    parent = {source: None}

    while open_list:
        _, current = heapq.heappop(open_list)

        if current == goal:
            path = []
            while current:
                path.append(current)
                current = parent[current]
            return path[::-1]

        for next_node, cost in graph[current]:
            new_g = cost_so_far[current] + cost
            if next_node not in cost_so_far or new_cost < cost_so_far[next_node]:
                cost_so_far[next_node] = new_cost
                heapq.heappush(open_list, (new_cost + H[next_node], next_node))
                parent[next_node] = current

    return None


n = int(input("Enter number of nodes: "))
nodes = input("Enter node names: ").split()

graph = {}
heuristic = {}

for current in nodes:
    heuristic[current] = int(input(f"Heuristic value of {current}: "))
    graph[current] = []

e = int(input("Enter number of edges: "))

for _ in range(e):
    u, v, c = input("Enter edge (from to cost): ").split()
    graph[u].append((v, int(c)))

source = input("Enter start node: ")
goal = input("Enter goal node: ")

path = astar(graph, heuristic, source, goal)

if path:
    print("Path found:", path)
else:
    print("No path found")
